---
import { whoWeServe } from '../../data/whoWeServe';
import { hasPersonaDetail } from '../../data/personaDetails';

interface Props {
  variant?: 'bold' | 'card' | 'cinema';
}

const { variant = 'bold' } = Astro.props;

const short: Record<string, string> = {
  marketing: 'Marketing',
  operations: 'Operations',
  technology: 'Technology',
  finance: 'Finance',
  facilities: 'Facilities',
  leadership: 'General management',
};
const teams = whoWeServe.teams.map((t, i) => ({ ...t, i, short: short[t.id] ?? t.label, linked: hasPersonaDetail(t.id as any) }));
const initialIdx = Math.max(0, teams.findIndex((t) => t.linked));
---

<div class:list={['trf', `trf--${variant}`]} data-teams-filmstrip data-initial={initialIdx}>
  <div class="trf__beam-wrap" aria-hidden="true">
    <div class="trf__beam"></div>
    <div class="trf__pulse"></div>
  </div>
  <div class="trf__stations">
    {teams.map((t) => (
      t.linked ? (
        <a
          href={`/who-we-serve/${t.id}`}
          class:list={['trf__station', t.i === initialIdx && 'is-active']}
          data-node={t.i}
          aria-label={`${t.short} team — open its page`}
        >
          <span class="trf__station-badge" set:html={t.icon} />
          <span class="trf__station-label">{t.short}</span>
        </a>
      ) : (
        <button
          type="button"
          class:list={['trf__station', t.i === initialIdx && 'is-active']}
          data-node={t.i}
          aria-pressed={t.i === initialIdx ? 'true' : 'false'}
          aria-label={`${t.short} team — coming soon`}
        >
          <span class="trf__station-badge" set:html={t.icon} />
          <span class="trf__station-label">{t.short}</span>
        </button>
      )
    ))}
  </div>

  <div class="trf__detail">
    {teams.map((t) => (
        <div class:list={['trf__entry', t.i === initialIdx && 'is-active']} data-entry={t.i}>
        <h3 class="trf__entry-label">{t.label}</h3>
        <p class="trf__entry-line">{t.line}</p>
        {t.linked ? (
          <a class="trf__entry-cta" href={`/who-we-serve/${t.id}`}>
            <span>Explore {t.short}</span>
            <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </a>
        ) : (
          <span class="trf__entry-soon">Coming soon</span>
        )}
      </div>
    ))}
  </div>
</div>

<style>
  .trf { width: 100%; max-width: 760px; margin-inline: auto; }
  .trf__beam-wrap { position: relative; height: 3px; margin: 0 26px 28px; border-radius: 3px; overflow: hidden; background: rgba(255, 255, 255, 0.08); }
  .trf__beam { position: absolute; inset: 0; background: linear-gradient(90deg, rgba(104,227,190,0.18), rgba(206,176,110,0.22)); }
  .trf__pulse {
    position: absolute; top: 0; bottom: 0; width: 18%;
    background: linear-gradient(90deg, transparent, var(--mint), rgba(206,176,110,0.85), transparent);
    filter: blur(2px);
    animation: trf-travel 3.6s linear infinite;
  }
  @keyframes trf-travel { from { left: -18%; } to { left: 100%; } }

  .trf__stations {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 8px;
    margin-bottom: clamp(28px, 4vw, 40px);
  }
  .trf__station {
    display: flex; flex-direction: column; align-items: center; gap: 10px;
    background: none; border: 0; padding: 4px; cursor: pointer; color: var(--off-white);
    text-decoration: none;
  }
  .trf__station-badge {
    position: relative;
    display: flex; align-items: center; justify-content: center;
    width: clamp(48px, 8vw, 64px); height: clamp(48px, 8vw, 64px);
    border-radius: 9999px;
    background: rgba(16, 35, 45, 0.75);
    border: 1px solid rgba(255, 255, 255, 0.16);
    color: rgba(245, 248, 250, 0.8);
    transition: all 0.2s ease;
  }
  .trf__station-badge :global(svg) { width: 46%; height: 46%; }
  .trf__station.is-active .trf__station-badge,
  .trf__station:hover .trf__station-badge,
  .trf__station:focus-visible .trf__station-badge {
    background: rgba(104, 227, 190, 0.18);
    border-color: var(--mint);
    color: var(--mint);
    box-shadow: 0 0 20px rgba(104, 227, 190, 0.28);
    transform: translateY(-4px);
  }
  .trf__station.is-active .trf__station-badge {
    box-shadow: 0 0 0 2px rgba(206, 176, 110, 0.6), 0 0 22px rgba(104, 227, 190, 0.32);
  }
  .trf__station-label {
    font-family: var(--font-body); font-size: 10px; font-weight: 700;
    letter-spacing: 0.08em; text-transform: uppercase; color: rgba(245, 248, 250, 0.7);
    text-align: center; line-height: 1.3; max-width: 12ch;
  }
  .trf__station.is-active .trf__station-label { color: var(--mint); }

  .trf__detail { text-align: center; min-height: 150px; }
  .trf__entry { display: none; }
  .trf__entry.is-active { display: block; animation: trf-pop 0.4s cubic-bezier(0.34, 1.56, 0.64, 1); }
  @keyframes trf-pop { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: none; } }
  .trf__entry-label { margin: 0 0 10px; font-family: var(--font-body); font-size: clamp(20px, 2.6vw, 26px); font-weight: 700; color: var(--off-white); }
  .trf__entry-line { margin: 0 auto 18px; max-width: 46ch; font-family: var(--font-body); font-size: 15px; line-height: 1.55; color: rgba(245, 248, 250, 0.78); }
  .trf__entry-cta {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 10px 18px; border-radius: 9999px;
    border: 1px solid var(--mint); background: rgba(104, 227, 190, 0.12);
    color: var(--mint);
    font-family: var(--font-body); font-size: 13px; font-weight: 700; letter-spacing: 0.02em;
    text-decoration: none; transition: all 0.2s ease;
  }
  .trf__entry-cta svg { width: 16px; height: 16px; transition: transform 0.15s ease; }
  .trf__entry-cta:hover { background: var(--mint); color: var(--navy-deep); }
  .trf__entry-cta:hover svg { transform: translateX(3px); }
  .trf__entry-soon { display: inline-block; padding: 5px 10px; font-size: 10px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: rgba(245, 248, 250, 0.55); border: 1px solid rgba(255, 255, 255, 0.14); border-radius: 9999px; }

  /* ---------- Variant A — Bold spine ---------- */
  .trf--bold { max-width: 1120px; }
  .trf--bold .trf__beam-wrap { height: 5px; margin-bottom: 32px; box-shadow: 0 0 14px rgba(104, 227, 190, 0.35); }
  .trf--bold .trf__pulse { width: 24%; animation-duration: 3.2s; }
  .trf--bold .trf__station-badge { width: clamp(72px, 9vw, 92px); height: clamp(72px, 9vw, 92px); }
  .trf--bold .trf__station-label { font-size: 11px; }
  .trf--bold .trf__station.is-active .trf__station-badge { transform: translateY(-6px) scale(1.08); box-shadow: 0 0 0 3px rgba(206, 176, 110, 0.6), 0 0 28px rgba(104, 227, 190, 0.42); }
  .trf--bold .trf__station.is-active .trf__station-badge::after {
    content: ''; position: absolute; inset: -6px; border-radius: 9999px; border: 1px solid rgba(104, 227, 190, 0.5);
    animation: trf-ring 1.8s ease-out infinite;
  }
  .trf--bold .trf__detail { min-height: 184px; }
  .trf--bold .trf__entry-label { font-size: clamp(26px, 3.2vw, 34px); }
  .trf--bold .trf__entry-line { font-size: 17px; max-width: 52ch; }

  /* ---------- Variant B — Card row ---------- */
  .trf--card { max-width: 1180px; }
  .trf--card .trf__beam-wrap { height: 4px; margin: 0 12px; box-shadow: 0 0 12px rgba(104, 227, 190, 0.3); }
  .trf--card .trf__stations { gap: 14px; margin-bottom: clamp(32px, 4vw, 44px); }
  .trf--card .trf__station { padding: 18px 12px 16px; border-radius: 14px; background: rgba(16, 35, 45, 0.6); border: 1px solid rgba(255, 255, 255, 0.1); gap: 12px; }
  .trf--card .trf__station-badge { width: clamp(52px, 7vw, 64px); height: clamp(52px, 7vw, 64px); }
  .trf--card .trf__station-label { font-size: 12px; max-width: 14ch; }
  .trf--card .trf__station.is-active { background: rgba(104, 227, 190, 0.1); border-color: rgba(206, 176, 110, 0.5); }
  .trf--card .trf__station.is-active .trf__station-badge { transform: translateY(-4px) scale(1.06); box-shadow: 0 0 0 2px rgba(206, 176, 110, 0.55), 0 0 22px rgba(104, 227, 190, 0.34); }
  .trf--card .trf__detail { min-height: 172px; }
  .trf--card .trf__entry-label { font-size: clamp(24px, 3vw, 32px); }
  .trf--card .trf__entry-line { font-size: 16px; max-width: 50ch; }

  /* ---------- Variant C — Cinematic ---------- */
  .trf--cinema { max-width: 1280px; }
  .trf--cinema .trf__beam-wrap { height: 8px; margin: 0 30px 40px; box-shadow: 0 0 22px rgba(104, 227, 190, 0.4); }
  .trf--cinema .trf__beam { background: linear-gradient(90deg, rgba(104, 227, 190, 0.22), rgba(206, 176, 110, 0.28)); }
  .trf--cinema .trf__pulse { width: 30%; animation-duration: 5s; filter: blur(3px); }
  .trf--cinema .trf__stations { gap: 10px; margin-bottom: clamp(36px, 5vw, 52px); }
  .trf--cinema .trf__station-badge { width: clamp(88px, 12vw, 112px); height: clamp(88px, 12vw, 112px); }
  .trf--cinema .trf__station-label { font-size: 12px; }
  .trf--cinema .trf__station.is-active .trf__station-badge { transform: translateY(-8px) scale(1.12); box-shadow: 0 0 0 3px rgba(206, 176, 110, 0.65), 0 0 36px rgba(104, 227, 190, 0.5); }
  .trf--cinema .trf__station.is-active .trf__station-badge::after {
    content: ''; position: absolute; inset: -10px; border-radius: 9999px; border: 1px solid rgba(104, 227, 190, 0.45);
    animation: trf-ring 2.4s ease-out infinite;
  }
  .trf--cinema .trf__detail { min-height: 204px; }
  .trf--cinema .trf__entry-label { font-size: clamp(30px, 4vw, 40px); }
  .trf--cinema .trf__entry-line { font-size: 18px; max-width: 54ch; }
  .trf--cinema .trf__entry.is-active { animation: trf-fade 0.5s ease; }

  @keyframes trf-ring { 0% { transform: scale(0.92); opacity: 0.8; } 100% { transform: scale(1.3); opacity: 0; } }
  @keyframes trf-fade { from { opacity: 0; } to { opacity: 1; } }

  @media (max-width: 640px) {
    .trf__stations { grid-template-columns: repeat(3, 1fr); row-gap: 20px; }
    .trf__beam-wrap { display: none; }
  }
  @media (prefers-reduced-motion: reduce) {
    .trf__pulse { animation: none; left: 0; width: 100%; opacity: 0.3; }
  }
</style>

<script>
  document.querySelectorAll<HTMLElement>('[data-teams-filmstrip]').forEach((root) => {
    const stations = Array.from(root.querySelectorAll<HTMLElement>('.trf__station'));
    const entries = Array.from(root.querySelectorAll<HTMLElement>('[data-entry]'));
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const initial = Number(root.getAttribute('data-initial')) || 0;
    let active = initial, pinned = false, timer: number | undefined;
    function set(i: number) {
      active = i;
      stations.forEach((n) => n.classList.toggle('is-active', n.getAttribute('data-node') === String(i)));
      entries.forEach((e) => e.classList.toggle('is-active', e.getAttribute('data-entry') === String(i)));
    }
    function start() { if (reduce || pinned) return; stop(); timer = window.setInterval(() => set((active + 1) % stations.length), 4500); }
    function stop() { if (timer) window.clearInterval(timer); }
    stations.forEach((n) => {
      const i = Number(n.getAttribute('data-node'));
      // Anchors navigate on click; buttons (coming soon) just pin the preview.
      n.addEventListener('click', () => { pinned = true; stop(); set(i); });
      n.addEventListener('mouseenter', () => { stop(); set(i); });
      n.addEventListener('mouseleave', start);
    });
    set(initial);
    if (!reduce) start();
  });
</script>
